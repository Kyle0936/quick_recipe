name: Auto-approve recipe-only PRs

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

jobs:
  approve:
    runs-on: ubuntu-latest
    if: github.event.pull_request.user.login != 'github-actions[bot]'
    steps:
      - name: Collect changed files
        id: files
        uses: actions/github-script@v7
        with:
          script: |
            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              per_page: 100
            });
            const names = files.map(f => f.filename);
            core.setOutput('names', JSON.stringify(names));
            const recipeOnly = names.length > 0 && names.every(n => /^recipes\/.*\.md$/.test(n));
            core.setOutput('recipe_only', recipeOnly ? 'true' : 'false');

      - name: Auto-approve
        if: steps.files.outputs.recipe_only == 'true'
        uses: actions/github-script@v7
        env:
          BOT_TOKEN: ${{ secrets.RECIPE_BOT_TOKEN }}
        with:
          script: |
            if (!process.env.BOT_TOKEN) {
              core.setFailed('Missing RECIPE_BOT_TOKEN secret');
              return;
            }
            const { owner, repo } = context.repo;
            const pull_number = context.payload.pull_request.number;
            await github.request('POST /repos/{owner}/{repo}/pulls/{pull_number}/reviews', {
              owner,
              repo,
              pull_number,
              event: 'APPROVE',
              body: 'Auto-approved: PR only changes recipe markdown files.'
            });
